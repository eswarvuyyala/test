pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git url: 'https://github.com/eswarvuyyala/react-app.git', branch: 'main'
            }
        }
    }
}
